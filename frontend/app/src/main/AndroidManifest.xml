<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GeoQuiz_frontend">
        <activity
            android:name=".UI.Auth.LoginActivity"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.Home.MainActivity"
            android:exported="false"/>
        <activity
            android:name=".UI.Profile.ProfileActivity"
            android:exported="false"/>
        <activity
            android:name=".UI.Profile.SettingsActivity"
            android:exported="false"/>
        <activity
            android:name=".UI.Game.GameModesActivity"
            android:exported="false"/>
        <activity
            android:name=".UI.Game.GameTypesActivity"
            android:exported="false"/>
        <activity
            android:name=".UI.Game.SoloGameActivity"
            android:exported="false"/>
        <activity
            android:name=".UI.Game.GameResultActivity"
            android:exported="false"/>
    </application>

</manifest>